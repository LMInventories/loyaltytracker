// Bump this whenever a static file under /public (icons, favicon, etc.) changes
// content but keeps the same filename/URL. Browsers and the service worker
// both cache those by URL, so an unchanged URL means an unchanged cached
// copy even after the file on disk is replaced.
export const ASSET_VERSION = "3";
